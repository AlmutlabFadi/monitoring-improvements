# تعليمات إعداد وتشغيل لوحة التحكم (Dashboard)

لوحة التحكم هي واجهة الويب التي تتيح لك مراقبة الأجهزة وعرض البيانات التي تم جمعها. تم تطويرها باستخدام React.

## المتطلبات المسبقة

تأكد من تثبيت البرامج التالية على جهازك:

-   **Node.js**: الإصدار 18 أو أحدث. يمكنك تنزيله من [nodejs.org](https://nodejs.org/).
-   **npm** (مدير حزم Node.js): يأتي مدمجًا مع Node.js.

## خطوات الإعداد والتشغيل

اتبع الخطوات التالية لإعداد وتشغيل لوحة التحكم:

1.  **انتقل إلى مجلد المشروع:**

    افتح الطرفية (Terminal) أو موجه الأوامر (Command Prompt) وانتقل إلى مجلد لوحة التحكم:

    ```bash
    cd monitoring-app/dashboard-v2
    ```

2.  **تثبيت التبعيات:**

    قم بتثبيت جميع الحزم والتبعيات المطلوبة للمشروع. قد يستغرق هذا بضع دقائق:

    ```bash
    npm install
    ```

3.  **تشغيل لوحة التحكم:**

    بعد تثبيت التبعيات بنجاح، يمكنك تشغيل لوحة التحكم في وضع التطوير. سيؤدي هذا إلى فتح لوحة التحكم في متصفح الويب الخاص بك:

    ```bash
    npm run dev
    ```

    ستظهر لك رسالة في الطرفية تشير إلى أن التطبيق يعمل على عنوان محلي، عادةً `http://localhost:5173` أو `http://localhost:3000`. افتح هذا العنوان في متصفح الويب الخاص بك.

4.  **بناء لوحة التحكم (للنشر):**

    إذا كنت ترغب في بناء نسخة جاهزة للنشر (Production) من لوحة التحكم، يمكنك استخدام الأمر التالي. سيتم إنشاء ملفات HTML و CSS و JavaScript مجمعة في مجلد `dist`:

    ```bash
    npm run build
    ```

    يمكنك بعد ذلك استضافة محتويات مجلد `dist` على أي خادم ويب ثابت (مثل Nginx, Apache, أو GitHub Pages).

## ملاحظات هامة

-   تأكد من أن الخادم الخلفي (Backend API) يعمل قبل تشغيل لوحة التحكم، حيث تعتمد لوحة التحكم على البيانات المقدمة من الخادم الخلفي.
-   إذا واجهت أي مشاكل، تأكد من أن Node.js و npm مثبتان بشكل صحيح وأنك في المجلد الصحيح للمشروع.

---

