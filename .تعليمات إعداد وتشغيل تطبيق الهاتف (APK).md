# تعليمات إعداد وتشغيل تطبيق الهاتف (APK)

تطبيق الهاتف (الذي يحمل اسم "البوصلة" للتمويه) هو تطبيق React Native. لبناء ملف APK قابل للتثبيت على أجهزة Android، ستحتاج إلى بيئة تطوير Android.

## المتطلبات المسبقة

تأكد من تثبيت البرامج التالية على جهازك:

-   **Node.js**: الإصدار 18 أو أحدث. يمكنك تنزيله من [nodejs.org](https://nodejs.org/).
-   **npm** (مدير حزم Node.js): يأتي مدمجًا مع Node.js.
-   **Java Development Kit (JDK)**: الإصدار 11 أو أحدث. يمكنك تنزيله من موقع Oracle أو OpenJDK.
-   **Android Studio**: لتثبيت Android SDK وإعداد المتغيرات البيئية. يمكنك تنزيله من [developer.android.com](https://developer.android.com/studio).
    -   بعد تثبيت Android Studio، تأكد من تثبيت **Android SDK Platform** و **Android SDK Build-Tools** (الإصدار 33 أو أحدث).
    -   قم بإعداد المتغيرات البيئية `ANDROID_HOME` و `PATH` لتشير إلى مجلد Android SDK.

## خطوات الإعداد والبناء (APK)

اتبع الخطوات التالية لإعداد وبناء ملف APK لتطبيق البوصلة:

1.  **انتقل إلى مجلد المشروع:**

    افتح الطرفية (Terminal) أو موجه الأوامر (Command Prompt) وانتقل إلى مجلد تطبيق البوصلة:

    ```bash
    cd monitoring-app/compass-app
    ```

2.  **تثبيت التبعيات:**

    قم بتثبيت جميع الحزم والتبعيات المطلوبة للمشروع:

    ```bash
    npm install
    ```

3.  **بناء ملف APK (للتجريب):**

    لبناء ملف APK Debug (غير موقع)، والذي يمكن استخدامه للتجريب على جهازك:

    ```bash
    npx react-native run-android
    ```
    
    **ملاحظة**: هذا الأمر سيقوم بتشغيل التطبيق على محاكي أو جهاز متصل. إذا كنت تريد فقط بناء الـ APK دون تشغيله مباشرة، يمكنك استخدام الأمر التالي:

    ```bash
    cd android && ./gradlew assembleDebug
    ```

    سيتم إنشاء ملف APK في المسار التالي (تقريبي):
    `monitoring-app/compass-app/android/app/build/outputs/apk/debug/app-debug.apk`

4.  **بناء ملف APK (للنشر - Signed APK):**

    لبناء ملف APK موقع (Signed APK) جاهز للنشر على متجر Google Play أو للتوزيع الآمن، ستحتاج إلى إنشاء مفتاح توقيع (Keystore) وتكوين Gradle. هذه عملية أكثر تعقيدًا وتتطلب خطوات إضافية:

    أ.  **إنشاء Keystore:**

        ```bash
        keytool -genkeypair -v -keystore my-upload-key.keystore -alias my-key-alias -keyalg RSA -keysize 2048 -validity 10000
        ```
        
        اتبع التعليمات لإنشاء المفتاح. احتفظ بملف `my-upload-key.keystore` في مكان آمن.

    ب.  **تكوين Gradle:**

        قم بإنشاء ملف `~/.gradle/gradle.properties` (إذا لم يكن موجودًا) وأضف معلومات Keystore:

        ```properties
        MYAPP_UPLOAD_STORE_FILE=my-upload-key.keystore
        MYAPP_UPLOAD_KEY_ALIAS=my-key-alias
        MYAPP_UPLOAD_STORE_PASSWORD=your_keystore_password
        MYAPP_UPLOAD_KEY_PASSWORD=your_key_password
        ```

        ثم قم بتعديل ملف `android/app/build.gradle` لإضافة تكوين التوقيع.

    ج.  **بناء Signed APK:**

        ```bash
        cd android && ./gradlew assembleRelease
        ```

        سيتم إنشاء ملف APK الموقع في المسار التالي (تقريبي):
        `monitoring-app/compass-app/android/app/build/outputs/apk/release/app-release.apk`

## ملاحظات هامة

-   تأكد من أن جميع المتغيرات البيئية لـ Android SDK مضبوطة بشكل صحيح.
-   إذا واجهت مشاكل، تحقق من وثائق React Native الرسمية للحصول على أحدث الإرشادات حول إعداد بيئة التطوير وبناء التطبيقات.
-   تطبيق البوصلة يتضمن وظائف مخفية. تذكر أن الوصول إليها يتم عبر 5 نقرات سريعة على إبرة البوصلة أو باستخدام كلمة المرور السرية `compass2025`.

---

