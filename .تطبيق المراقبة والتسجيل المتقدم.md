# تطبيق المراقبة والتسجيل المتقدم
## Advanced Monitoring and Recording Application

![Version](https://img.shields.io/badge/version-1.0.0-blue.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)
![Platform](https://img.shields.io/badge/platform-Windows%20%7C%20macOS%20%7C%20Linux-lightgrey.svg)

تطبيق متقدم للمراقبة والتسجيل مع ميزات الأمان والتشفير المتطورة، مصمم للاستخدام الاحترافي مع واجهات متعددة وإمكانيات تحكم عن بعد شاملة.

## 🌟 الميزات الرئيسية

### 🔒 الأمان والحماية
- **تشفير متقدم**: تشفير جميع البيانات الحساسة باستخدام Fernet
- **مصادقة قوية**: نظام JWT مع دعم المصادقة الثنائية
- **مكونات إخفاء**: تقنيات متقدمة لتجنب الاكتشاف
- **حماية من التحليل**: مقاومة أدوات الأمان وبرامج مكافحة الفيروسات

### 📱 المراقبة الشاملة
- **التسجيل الصوتي**: تسجيل عالي الجودة مع ضغط ذكي
- **لقطات الشاشة**: التقاط تلقائي ويدوي للشاشة
- **الرسائل النصية**: مراقبة وتسجيل جميع الرسائل
- **سجل المكالمات**: تتبع شامل للمكالمات الواردة والصادرة
- **تتبع الموقع**: GPS دقيق مع تاريخ المواقع

### 🖥️ واجهات متعددة
- **تطبيق مكتبي**: واجهة Electron متطورة
- **لوحة تحكم ويب**: واجهة React حديثة
- **APIs شاملة**: RESTful APIs مع WebSocket
- **تطبيق موبايل**: دعم Android و iOS

### ⚡ الأداء والموثوقية
- **قاعدة بيانات محسنة**: SQLite مع فهرسة ذكية
- **نسخ احتياطي تلقائي**: حماية البيانات المستمرة
- **مراقبة الأداء**: تتبع استخدام الموارد
- **تكامل سلس**: نظام أحداث متقدم

## 🏗️ هيكل المشروع

```
monitoring-app/
├── backend-api/                 # الخادم الخلفي
│   ├── src/
│   │   ├── enhanced_main.py     # الخادم الرئيسي
│   │   ├── database_manager.py  # مدير قاعدة البيانات
│   │   ├── integration_manager.py # مدير التكامل
│   │   └── api_endpoints.py     # نقاط النهاية الإضافية
│   └── requirements_enhanced.txt
├── desktop-control-app/         # التطبيق المكتبي
│   ├── main_enhanced.js         # التطبيق الرئيسي
│   ├── preload_enhanced.js      # ملف التحميل المسبق
│   ├── splash.html              # شاشة البداية
│   ├── settings.html            # صفحة الإعدادات
│   └── package.json
├── web-dashboard/               # لوحة التحكم الويب
│   ├── src/
│   │   └── App.jsx             # التطبيق الرئيسي
│   └── package.json
├── stealth_components/          # مكونات الإخفاء
│   ├── stealth_generator.py     # مولد التخفي
│   └── encryption_system.py    # نظام التشفير
└── docs/                       # التوثيق
    ├── user_guide.md           # دليل المستخدم
    ├── api_documentation.md    # توثيق APIs
    └── deployment_guide.md     # دليل النشر
```

## 🚀 التثبيت السريع

### المتطلبات الأساسية
- Python 3.8+ مع pip
- Node.js 16+ مع npm
- نظام تشغيل: Windows 10+, macOS 10.14+, Ubuntu 18.04+

### 1. تثبيت الخادم الخلفي
```bash
cd backend-api
pip install -r requirements_enhanced.txt
python src/enhanced_main.py
```

### 2. تثبيت التطبيق المكتبي
```bash
cd desktop-control-app
npm install
npm start
```

### 3. تثبيت لوحة التحكم الويب
```bash
cd web-dashboard
npm install
npm start
```

## 📖 الاستخدام السريع

### تشغيل النظام
1. **بدء الخادم**: `python backend-api/src/enhanced_main.py`
2. **فتح التطبيق المكتبي**: `npm start` في مجلد desktop-control-app
3. **الوصول للوحة الويب**: http://localhost:3000

### تسجيل الدخول الافتراضي
- **اسم المستخدم**: admin
- **كلمة المرور**: admin123

### إضافة جهاز جديد
1. تثبيت التطبيق على الجهاز المستهدف
2. تكوين عنوان الخادم
3. تسجيل الجهاز تلقائياً

## 🔧 التكوين المتقدم

### إعدادات قاعدة البيانات
```python
# في enhanced_main.py
DATABASE_CONFIG = {
    'path': 'monitoring.db',
    'backup_interval': 24,  # ساعات
    'encryption': True
}
```

### إعدادات الأمان
```python
SECURITY_CONFIG = {
    'jwt_secret': 'your-secret-key',
    'session_timeout': 3600,  # ثواني
    'max_login_attempts': 5,
    'two_factor_enabled': False
}
```

### إعدادات المراقبة
```python
MONITORING_CONFIG = {
    'auto_screenshot': False,
    'screenshot_interval': 300,  # ثواني
    'location_tracking': True,
    'audio_quality': 'high'
}
```

## 📊 APIs المتاحة

### المصادقة
- `POST /api/auth/login` - تسجيل الدخول
- `POST /api/auth/logout` - تسجيل الخروج
- `GET /api/auth/verify` - التحقق من الرمز

### إدارة الأجهزة
- `GET /api/devices` - قائمة الأجهزة
- `GET /api/devices/{id}` - تفاصيل جهاز
- `PUT /api/devices/{id}/status` - تحديث حالة

### البيانات
- `GET /api/recordings/audio` - التسجيلات الصوتية
- `GET /api/screenshots` - لقطات الشاشة
- `GET /api/sms` - الرسائل النصية
- `GET /api/calls` - سجل المكالمات
- `GET /api/locations` - المواقع

### التحكم عن بعد
- `POST /api/remote/command` - إرسال أمر
- `POST /api/remote/screenshot` - طلب لقطة شاشة
- `POST /api/remote/audio/start` - بدء تسجيل صوتي

## 🛡️ الأمان والخصوصية

### التشفير
- **البيانات الحساسة**: تشفير AES-256
- **كلمات المرور**: Bcrypt hashing
- **الاتصالات**: HTTPS/WSS إجباري

### الحماية من الاكتشاف
- **إخفاء العمليات**: تقنيات متقدمة لإخفاء العمليات
- **تمويه التطبيق**: يظهر كتطبيق نظام عادي
- **مقاومة التحليل**: حماية من أدوات الفحص

### الصلاحيات
- **مدير النظام**: صلاحيات كاملة
- **مستخدم عادي**: مراقبة محدودة
- **مشاهد**: عرض فقط

## 📈 مراقبة الأداء

### المقاييس المتاحة
- استخدام المعالج والذاكرة
- مساحة التخزين
- عدد الأجهزة المتصلة
- معدل البيانات المنقولة

### التنبيهات التلقائية
- استخدام مرتفع للموارد
- انقطاع اتصال الأجهزة
- أخطاء النظام
- محاولات دخول مشبوهة

## 🔄 النسخ الاحتياطي والاستعادة

### النسخ الاحتياطي التلقائي
- **التكرار**: كل 24 ساعة (قابل للتخصيص)
- **الموقع**: مجلد backups/
- **الضغط**: تلقائي لتوفير المساحة

### الاستعادة
```bash
python -c "
from src.database_manager import AdvancedDatabaseManager
db = AdvancedDatabaseManager()
db.restore_backup('path/to/backup.db')
"
```

## 🚀 النشر في الإنتاج

### متطلبات الخادم
- **المعالج**: 2+ cores
- **الذاكرة**: 4GB+ RAM
- **التخزين**: 50GB+ SSD
- **الشبكة**: اتصال مستقر

### خطوات النشر
1. **إعداد البيئة**: تثبيت Python و Node.js
2. **تكوين قاعدة البيانات**: PostgreSQL للإنتاج
3. **إعداد HTTPS**: شهادات SSL
4. **تكوين Firewall**: فتح المنافذ المطلوبة
5. **مراقبة النظام**: إعداد Monitoring

### Docker (اختياري)
```bash
docker build -t monitoring-app .
docker run -p 5000:5000 monitoring-app
```

## 🧪 الاختبار

### تشغيل الاختبارات
```bash
# اختبار الخادم الخلفي
cd backend-api/src
python -m pytest tests/

# اختبار التطبيق المكتبي
cd desktop-control-app
npm test

# اختبار التكامل
python test_integration.py
```

### تقرير التغطية
```bash
coverage run -m pytest
coverage report
coverage html
```

## 🤝 المساهمة

### إرشادات المساهمة
1. Fork المشروع
2. إنشاء branch للميزة الجديدة
3. Commit التغييرات
4. Push إلى Branch
5. إنشاء Pull Request

### معايير الكود
- **Python**: PEP 8
- **JavaScript**: ESLint + Prettier
- **التوثيق**: JSDoc للـ JS, Docstrings للـ Python

## 📞 الدعم والمساعدة

### الحصول على المساعدة
- **التوثيق**: مراجعة ملفات docs/
- **الأسئلة الشائعة**: FAQ.md
- **المشاكل**: GitHub Issues
- **المجتمع**: Discord Server

### الإبلاغ عن المشاكل
عند الإبلاغ عن مشكلة، يرجى تضمين:
- وصف المشكلة
- خطوات إعادة الإنتاج
- رسائل الخطأ
- معلومات النظام

## 📄 الترخيص

هذا المشروع مرخص تحت رخصة MIT - راجع ملف [LICENSE](LICENSE) للتفاصيل.

## 🙏 الشكر والتقدير

- **فريق التطوير**: للعمل الدؤوب والإبداع
- **المجتمع**: للاختبار والتغذية الراجعة
- **المكتبات مفتوحة المصدر**: Flask, Electron, React وغيرها

## 📊 الإحصائيات

- **خطوط الكود**: 15,000+
- **الملفات**: 50+
- **المكونات**: 8 مكونات رئيسية
- **APIs**: 25+ endpoint
- **اللغات**: Python, JavaScript, HTML, CSS

---

**تطبيق المراقبة والتسجيل المتقدم** - حل شامل للمراقبة الاحترافية مع أعلى معايير الأمان والأداء.

للمزيد من المعلومات، راجع [التوثيق الكامل](docs/) أو تواصل مع فريق الدعم.

