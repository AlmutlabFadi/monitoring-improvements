# تعليمات إعداد وتشغيل الخادم الخلفي (Backend API)

الخادم الخلفي هو جوهر نظام المراقبة، حيث يتعامل مع جمع البيانات، معالجتها، وتخزينها، ويوفر واجهات برمجة التطبيقات (APIs) للوحة التحكم وتطبيق الهاتف. تم تطويره باستخدام Flask.

## المتطلبات المسبقة

تأكد من تثبيت البرامج التالية على جهازك:

-   **Python**: الإصدار 3.9 أو أحدث. يمكنك تنزيله من [python.org](https://www.python.org/).
-   **pip** (مدير حزم Python): يأتي مدمجًا مع Python.

## خطوات الإعداد والتشغيل

اتبع الخطوات التالية لإعداد وتشغيل الخادم الخلفي:

1.  **انتقل إلى مجلد المشروع:**

    افتح الطرفية (Terminal) أو موجه الأوامر (Command Prompt) وانتقل إلى مجلد الخادم الخلفي:

    ```bash
    cd monitoring-app/backend-api
    ```

2.  **إنشاء بيئة افتراضية (اختياري ولكن موصى به):**

    يُنصح بإنشاء بيئة افتراضية لعزل تبعيات المشروع عن تثبيت Python العام:

    ```bash
    python3 -m venv venv
    ```

    ثم قم بتنشيط البيئة الافتراضية:

    -   **لنظام التشغيل Linux/macOS:**

        ```bash
        source venv/bin/activate
        ```

    -   **لنظام التشغيل Windows (Command Prompt):**

        ```bash
        venv\Scripts\activate.bat
        ```

    -   **لنظام التشغيل Windows (PowerShell):**

        ```powershell
        .\venv\Scripts\Activate.ps1
        ```

3.  **تثبيت التبعيات:**

    قم بتثبيت جميع الحزم والتبعيات المطلوبة للخادم الخلفي:

    ```bash
    pip install -r requirements_enhanced.txt
    ```

4.  **تشغيل الخادم الخلفي:**

    بعد تثبيت التبعيات بنجاح، يمكنك تشغيل الخادم الخلفي. تأكد من أنك داخل مجلد `backend-api/src`:

    ```bash
    cd src
    python3 enhanced_main.py
    ```

    أو إذا كنت تستخدم `flask` مباشرة:

    ```bash
    export FLASK_APP=enhanced_main.py
    export FLASK_ENV=development # لبيئة التطوير
    flask run --host=0.0.0.0 --port=5000
    ```

    سيتم تشغيل الخادم على المنفذ 5000 (افتراضيًا) وسيكون متاحًا على `http://localhost:5000`.

## ملاحظات هامة

-   **قاعدة البيانات:** يستخدم الخادم الخلفي قاعدة بيانات SQLite بشكل افتراضي (ملفات `.db` ستُنشأ تلقائيًا في مجلد المشروع). لا تحتاج إلى إعداد قاعدة بيانات خارجية في البداية.
-   **الوصول من الخارج:** إذا كنت تخطط للوصول إلى الخادم من أجهزة أخرى في شبكتك أو من الإنترنت، تأكد من أن جدار الحماية (Firewall) يسمح بالاتصالات على المنفذ 5000.
-   **البيانات:** سيقوم الخادم بإنشاء مجلدات لتخزين البيانات مثل `camera_captures` و `social_media`.

---

